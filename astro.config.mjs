// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static',
  // site: 'https://hangroup.mit.edu', // TODO: set once final hosting URL is decided
});
